import { Insight } from '@/types/api/insights';
import { InsightContext } from '../types';

export function generateFrictionThemesInsight(
  ctx: InsightContext
): Insight | null {
  // TODO: aggregate firstPassFeedbackTags from summaries
  return null;
}
